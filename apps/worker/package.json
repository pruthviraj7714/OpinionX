{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "build": "bun build index.ts --outdir dist --target bun",
    "start": "bun dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/db" : "*",
    "@repo/redisclient" : "*"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bullmq": "^5.66.2"
  }
}
